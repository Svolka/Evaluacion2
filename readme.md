# Evaluacion2

Este repositorio contiene el programa **geolocalizacion.py**, desarrollado para la Evaluación 2 del módulo de Python en la VM DEVSAC.  
El programa utiliza la **API de GraphHopper Directions** para calcular rutas entre dos ubicaciones geográficas ingresadas por el usuario.

## 🗺️ Descripción del Proyecto
El software permite al usuario ingresar coordenadas de **origen** y **destino** (latitud y longitud) para obtener:
- La distancia total del recorrido (en km).
- El tiempo estimado de viaje (en minutos).
- Las instrucciones paso a paso del trayecto, todo en **idioma español**.

Además:
- Todos los textos de interacción están traducidos al español.
- Los valores numéricos se muestran con **dos decimales**.
- Se puede salir del programa ingresando `s` o `salir`.

## ⚙️ Requisitos
- Python 3.x instalado  
- Librería `requests` instalada


